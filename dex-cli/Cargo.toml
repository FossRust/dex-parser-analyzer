[package]
name = "dex-cli"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1.0"
clap = { version = "4.5", features = ["derive"] }
dex-core = { path = "../dex-core" }
dex-analysis = { path = "../dex-analysis" }
